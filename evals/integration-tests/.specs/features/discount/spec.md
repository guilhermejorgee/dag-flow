# Discount Extractor

A utility function `getDiscount(text)` that parses a string.
If the string contains the word 'PROMO', it should return the exact word immediately following it.
If 'PROMO' is not found, or it is the last word in the string, it should return `null`.
It should be exported.
